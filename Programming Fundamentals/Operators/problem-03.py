# Q3. You have a = 10.0 and b = 3. What is the difference between a / b and a // b? Write code to verify.

a = 10.0
b = 3

print(a / b)
print(a // b)

# (a/b) always returns a float
# (a//b) rounds down to the nearest whole number